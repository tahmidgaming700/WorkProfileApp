<Window x:Class="VmManagerWpf.VmSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VM Settings" Height="250" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="VM Name:" VerticalAlignment="Center"/>
        <TextBox x:Name="VmNameTextBox" Grid.Column="1" Margin="6"/>

        <TextBlock Grid.Row="1" Text="RAM (MB):" VerticalAlignment="Center"/>
        <TextBox x:Name="RamTextBox" Grid.Row="1" Grid.Column="1" Margin="6"/>

        <TextBlock Grid.Row="2" Text="CPU Cores:" VerticalAlignment="Center"/>
        <TextBox x:Name="CpuTextBox" Grid.Row="2" Grid.Column="1" Margin="6"/>

        <TextBlock Grid.Row="3" Text="Disk (GB):" VerticalAlignment="Center"/>
        <TextBox x:Name="DiskTextBox" Grid.Row="3" Grid.Column="1" Margin="6"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Save" Width="80" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
